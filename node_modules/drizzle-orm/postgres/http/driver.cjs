Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
const require_runtime = require('../../_virtual/_rolldown/runtime.cjs');
const require_postgres_http_driver_core = require('./driver-core.cjs');
let minipg_http = require("minipg/http");

//#region src/postgres/http/driver.ts
const WIRE_PARITY = {
	temporal: "string",
	int8: "bigint"
};
function pinWireParity(client) {
	const cfg = client.cfg;
	if (cfg) Object.assign(cfg, WIRE_PARITY);
	return client;
}
const runBatch = (client) => (queries, options) => client.batch(queries, options);
function drizzle(...params) {
	if (typeof params[0] === "string") {
		const instance = (0, minipg_http.client)({
			url: params[0],
			...WIRE_PARITY
		});
		return require_postgres_http_driver_core.construct(instance, runBatch(instance), params[1]);
	}
	const { connection, client, ...config } = params[0];
	if (client) return require_postgres_http_driver_core.construct(pinWireParity(client), runBatch(client), config);
	const instance = typeof connection === "string" ? (0, minipg_http.client)({
		url: connection,
		...WIRE_PARITY
	}) : (0, minipg_http.client)({
		...WIRE_PARITY,
		...connection
	});
	return require_postgres_http_driver_core.construct(instance, runBatch(instance), config);
}
(function(_drizzle) {
	function mock(config) {
		return require_postgres_http_driver_core.construct({}, async () => [], config);
	}
	_drizzle.mock = mock;
})(drizzle || (drizzle = {}));

//#endregion
Object.defineProperty(exports, 'drizzle', {
  enumerable: true,
  get: function () {
    return drizzle;
  }
});
//# sourceMappingURL=driver.cjs.map
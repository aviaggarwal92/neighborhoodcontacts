import { minipgShapeCodecs } from "../codecs.js";
import { buildShape } from "../shape.js";
import { PostgresHttpSession } from "./session.js";
import { entityKind } from "../../entity.js";
import { jitCompatCheck } from "../../utils.js";
import { DefaultLogger } from "../../logger.js";
import { PgAsyncDatabase } from "../../pg-core/async/db.js";
import { PgDialect } from "../../pg-core/dialect.js";

//#region src/postgres/http/driver-core.ts
var PostgresHttpDatabase = class extends PgAsyncDatabase {
	static [entityKind] = "PostgresHttpDatabase";
	async batch(batch, options) {
		return this.session.batch(batch, options);
	}
};
/** @internal */
function construct(client, runBatch, config = {}) {
	const clientConfig = client.cfg ?? client;
	if (config.codecs && clientConfig?.temporal) clientConfig.temporal = "string";
	const dialect = new PgDialect({
		codecs: config.codecs ?? minipgShapeCodecs,
		useJitMappers: jitCompatCheck(config.jit),
		shapeGenerator: config.codecs ? void 0 : buildShape
	});
	let logger;
	if (config.logger === true) logger = new DefaultLogger();
	else if (config.logger !== false) logger = config.logger;
	const relations = config.relations ?? {};
	const db = new PostgresHttpDatabase(dialect, new PostgresHttpSession(client, runBatch, dialect, relations, {
		logger,
		cache: config.cache
	}), relations);
	db.$client = client;
	db.$cache = config.cache;
	if (db.$cache) db.$cache["invalidate"] = config.cache?.onMutate;
	return db;
}

//#endregion
export { PostgresHttpDatabase, construct };
//# sourceMappingURL=driver-core.js.map
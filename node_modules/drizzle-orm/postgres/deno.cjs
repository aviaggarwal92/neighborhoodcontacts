Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
const require_runtime = require('../_virtual/_rolldown/runtime.cjs');
const require_postgres_driver_core = require('./driver-core.cjs');
let minipg = require("minipg");
let minipg_deno = require("minipg/deno");

//#region src/postgres/deno.ts
function resolveUrl(config) {
	if (!config.url) return config;
	const out = { ...(0, minipg.parseConnectionString)(config.url) };
	for (const key of Object.keys(config)) {
		if (key === "url") continue;
		const value = config[key];
		if (value !== void 0) out[key] = value;
	}
	return out;
}
function drizzle(...params) {
	if (typeof params[0] === "string") return require_postgres_driver_core.construct((0, minipg_deno.createPool)(resolveUrl({
		url: params[0],
		temporal: "string"
	})), params[1]);
	const { connection, client, ...config } = params[0];
	if (client) return require_postgres_driver_core.construct(client, config);
	return require_postgres_driver_core.construct(typeof connection === "string" ? (0, minipg_deno.createPool)(resolveUrl({ url: connection })) : (0, minipg_deno.createPool)(resolveUrl({ ...connection })), config);
}
(function(_drizzle) {
	function mock(config) {
		return require_postgres_driver_core.construct({}, config);
	}
	_drizzle.mock = mock;
})(drizzle || (drizzle = {}));

//#endregion
Object.defineProperty(exports, 'drizzle', {
  enumerable: true,
  get: function () {
    return drizzle;
  }
});
//# sourceMappingURL=deno.cjs.map
Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
const require_runtime = require('../../_virtual/_rolldown/runtime.cjs');
const require_postgres_codecs = require('../codecs.cjs');
const require_postgres_aurora_session = require('./session.cjs');
let __entity_ts = require("../../entity.cjs");
let __utils_ts = require("../../utils.cjs");
let __logger_ts = require("../../logger.cjs");
let __pg_core_async_db_ts = require("../../pg-core/async/db.cjs");
let __pg_core_dialect_ts = require("../../pg-core/dialect.cjs");
let minipg_aurora = require("minipg/aurora");

//#region src/postgres/aurora/driver.ts
var PostgresAuroraDialect = class extends __pg_core_dialect_ts.PgDialect {
	static [__entity_ts.entityKind] = "PostgresAuroraDialect";
	escapeParam(num) {
		return `:p${num + 1}`;
	}
};
var PostgresAuroraDatabase = class extends __pg_core_async_db_ts.PgAsyncDatabase {
	static [__entity_ts.entityKind] = "PostgresAuroraDatabase";
};
function construct(client, config = {}) {
	if (client?.temporal) client.temporal = "string";
	const dialect = new PostgresAuroraDialect({
		codecs: config.codecs ?? require_postgres_codecs.minipgCodecs,
		useJitMappers: (0, __utils_ts.jitCompatCheck)(config.jit)
	});
	let logger;
	if (config.logger === true) logger = new __logger_ts.DefaultLogger();
	else if (config.logger !== false) logger = config.logger;
	const relations = config.relations ?? {};
	const db = new PostgresAuroraDatabase(dialect, new require_postgres_aurora_session.PostgresAuroraSession(client, dialect, relations, {
		logger,
		cache: config.cache
	}), relations);
	db.$client = client;
	db.$cache = config.cache;
	if (db.$cache) db.$cache["invalidate"] = config.cache?.onMutate;
	return db;
}
function drizzle(...params) {
	const { connection, client, ...config } = params[0];
	if (client) return construct(client, config);
	return construct((0, minipg_aurora.createPool)({
		...connection,
		temporal: "string"
	}), config);
}
(function(_drizzle) {
	function mock(config) {
		return construct({}, config);
	}
	_drizzle.mock = mock;
})(drizzle || (drizzle = {}));

//#endregion
exports.PostgresAuroraDatabase = PostgresAuroraDatabase;
exports.PostgresAuroraDialect = PostgresAuroraDialect;
Object.defineProperty(exports, 'drizzle', {
  enumerable: true,
  get: function () {
    return drizzle;
  }
});
//# sourceMappingURL=driver.cjs.map
import { construct } from "./driver-core.js";
import { createPool } from "minipg/neon-ws";

//#region src/postgres/neon-ws.ts
function drizzle(...params) {
	if (typeof params[0] === "string") return construct(createPool({
		url: params[0],
		temporal: "string"
	}), params[1]);
	const { connection, client, ...config } = params[0];
	if (client) return construct(client, config);
	return construct(typeof connection === "string" ? createPool({ url: connection }) : createPool({ ...connection }), config);
}
(function(_drizzle) {
	function mock(config) {
		return construct({}, config);
	}
	_drizzle.mock = mock;
})(drizzle || (drizzle = {}));

//#endregion
export { drizzle };
//# sourceMappingURL=neon-ws.js.map
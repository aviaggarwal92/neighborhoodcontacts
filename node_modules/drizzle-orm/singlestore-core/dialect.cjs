Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
const require_runtime = require('../_virtual/_rolldown/runtime.cjs');
const require_singlestore_core_columns_common = require('./columns/common.cjs');
const require_singlestore_core_table = require('./table.cjs');
let __entity_ts = require("../entity.cjs");
let __subquery_ts = require("../subquery.cjs");
let __view_common_ts = require("../view-common.cjs");
let __table_ts = require("../table.cjs");
let __column_ts = require("../column.cjs");
let __sql_sql_ts = require("../sql/sql.cjs");
let ___relations_ts = require("../_relations.cjs");
___relations_ts = require_runtime.__toESM(___relations_ts);
let __relations_ts = require("../relations.cjs");
let __migrator_utils_ts = require("../migrator.utils.cjs");
let __alias_ts = require("../alias.cjs");
let __errors_ts = require("../errors.cjs");
let __sql_expressions_index_ts = require("../sql/expressions/index.cjs");
let __up_migrations_singlestore_ts = require("../up-migrations/singlestore.cjs");

//#region src/singlestore-core/dialect.ts
var SingleStoreDialect = class {
	static [__entity_ts.entityKind] = "SingleStoreDialect";
	constructor(_config) {}
	async migrate(migrations, session, config) {
		const migrationsTable = config.migrationsTable ?? "__drizzle_migrations";
		const { newDb } = await (0, __up_migrations_singlestore_ts.upgradeIfNeeded)(migrationsTable, session, migrations);
		if (newDb) {
			const migrationTableCreate = __sql_sql_ts.sql`
			CREATE TABLE IF NOT EXISTS ${__sql_sql_ts.sql.identifier(migrationsTable)} (
				id SERIAL PRIMARY KEY,
				hash TEXT NOT NULL,
				created_at BIGINT,
				name TEXT,
				applied_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
			)
		`;
			await session.execute(migrationTableCreate);
		}
		const dbMigrations = await session.all(__sql_sql_ts.sql`select id, hash, created_at, name from ${__sql_sql_ts.sql.identifier(migrationsTable)}`);
		if (typeof config === "object" && config.init) {
			if (dbMigrations.length) return { exitCode: "databaseMigrations" };
			if (migrations.length > 1) return { exitCode: "localMigrations" };
			const [migration] = migrations;
			if (!migration) return;
			await session.execute(__sql_sql_ts.sql`insert into ${__sql_sql_ts.sql.identifier(migrationsTable)} (\`hash\`, \`created_at\`, \`name\`) values(${migration.hash}, ${migration.folderMillis}, ${migration.name})`);
			return;
		}
		const migrationsToRun = (0, __migrator_utils_ts.getMigrationsToRun)({
			localMigrations: migrations,
			dbMigrations
		});
		await session.transaction(async (tx) => {
			for (const migration of migrationsToRun) {
				for (const stmt of migration.sql) await tx.execute(__sql_sql_ts.sql.raw(stmt));
				await tx.execute(__sql_sql_ts.sql`insert into ${__sql_sql_ts.sql.identifier(migrationsTable)} (\`hash\`, \`created_at\`, \`name\`) values(${migration.hash}, ${migration.folderMillis}, ${migration.name})`);
			}
		});
	}
	escapeName(name) {
		return `\`${name.replace(/`/g, "``")}\``;
	}
	escapeParam(_num) {
		return `?`;
	}
	escapeString(str) {
		return `'${str.replace(/'/g, "''")}'`;
	}
	buildWithCTE(queries) {
		if (!queries?.length) return void 0;
		const queriesLen = queries.length;
		const withSqlChunks = new Array(queriesLen + 1);
		let writeIdx = 0;
		withSqlChunks[writeIdx++] = new __sql_sql_ts.StringChunk("with ");
		for (let i = 0; i < queriesLen; ++i) {
			const w = queries[i];
			withSqlChunks[writeIdx++] = i < queriesLen - 1 ? __sql_sql_ts.sql`${__sql_sql_ts.sql.identifier(w._.alias)} as (${w._.sql}), ` : __sql_sql_ts.sql`${__sql_sql_ts.sql.identifier(w._.alias)} as (${w._.sql}) `;
		}
		return new __sql_sql_ts.SQL(withSqlChunks);
	}
	buildDeleteQuery({ table, where, returning, withList, limit, orderBy }) {
		const withSql = this.buildWithCTE(withList);
		const returningSql = returning ? __sql_sql_ts.sql` returning ${this.buildSelection(returning, {
			isSingleTable: true,
			table
		})}` : void 0;
		return __sql_sql_ts.sql`${withSql}delete from ${table}${where ? __sql_sql_ts.sql` where ${where}` : void 0}${this.buildOrderBy(orderBy)}${this.buildLimit(limit)}${returningSql}`;
	}
	buildUpdateSet(table, set) {
		const tableColumns = table[__table_ts.Table.Symbol.Columns];
		const columnNames = Object.keys(tableColumns).filter((colName) => set[colName] !== void 0 || tableColumns[colName]?.onUpdateFn !== void 0);
		const setLength = columnNames.length;
		const setArr = new Array(setLength);
		for (let i = 0; i < columnNames.length; ++i) {
			const colName = columnNames[i];
			const col = tableColumns[colName];
			let value;
			if (set[colName] !== void 0) value = set[colName];
			else {
				const updateRes = col.onUpdateFn?.();
				value = (0, __entity_ts.is)(updateRes, __sql_sql_ts.SQL) ? updateRes : __sql_sql_ts.sql.param(updateRes, col);
			}
			setArr[i] = i < setLength - 1 ? __sql_sql_ts.sql`${__sql_sql_ts.sql.identifier(col.name)} = ${value}, ` : __sql_sql_ts.sql`${__sql_sql_ts.sql.identifier(col.name)} = ${value}`;
		}
		return new __sql_sql_ts.SQL(setArr);
	}
	buildUpdateQuery({ table, set, where, returning, withList, limit, orderBy }) {
		const withSql = this.buildWithCTE(withList);
		const setSql = this.buildUpdateSet(table, set);
		const returningSql = returning ? __sql_sql_ts.sql` returning ${this.buildSelection(returning, {
			isSingleTable: true,
			table
		})}` : void 0;
		return __sql_sql_ts.sql`${withSql}update ${table} set ${setSql}${where ? __sql_sql_ts.sql` where ${where}` : void 0}${this.buildOrderBy(orderBy)}${this.buildLimit(limit)}${returningSql}`;
	}
	/**
	* Builds selection SQL with provided fields/expressions
	*
	* Examples:
	*
	* `select <selection> from`
	*
	* `insert ... returning <selection>`
	*
	* If `isSingleTable` is true, then columns won't be prefixed with table name
	*/
	buildSelection(fields, { isSingleTable = false, table } = {}) {
		const { length: columnsLen } = fields;
		const chunks = [];
		const tableName = table ? (0, __entity_ts.is)(table, __sql_sql_ts.SQL) || (0, __entity_ts.is)(table, __subquery_ts.Subquery) ? void 0 : table[__table_ts.Table.Symbol.IsAlias] || table[__table_ts.Table.Symbol.Schema] === void 0 ? table[__table_ts.Table.Symbol.Name] : `${table[__table_ts.Table.Symbol.Schema]}.${table[__table_ts.Table.Symbol.Name]}` : void 0;
		for (let i = 0; i < columnsLen; ++i) {
			const { field, fieldType } = fields[i];
			switch (fieldType) {
				case "Column":
					if (isSingleTable) chunks.push(field.isAlias ? __sql_sql_ts.sql`${__sql_sql_ts.sql.identifier((0, __alias_ts.getOriginalColumnFromAlias)(field).name)} as ${field}` : __sql_sql_ts.sql.identifier(field.name));
					else chunks.push(field.isAlias ? __sql_sql_ts.sql`${(0, __alias_ts.getOriginalColumnFromAlias)(field)} as ${field}` : field);
					break;
				case "SQL.Aliased":
					if (field.isSelectionField) {
						if (!isSingleTable && field.origin !== void 0) chunks.push(__sql_sql_ts.sql.identifier(field.origin), new __sql_sql_ts.StringChunk("."));
						chunks.push(__sql_sql_ts.sql.identifier(field.fieldAlias));
					} else {
						if (isSingleTable && tableName !== void 0) {
							const { queryChunks } = field.sql;
							const newChunks = new Array(queryChunks.length);
							let abort = false;
							for (let i = 0; i < queryChunks.length; ++i) {
								const c = queryChunks[i];
								if ((0, __entity_ts.is)(c, __column_ts.Column)) {
									const { table } = c;
									if ((table[__table_ts.Table.Symbol.IsAlias] || table[__table_ts.Table.Symbol.Schema] === void 0 ? table[__table_ts.Table.Symbol.Name] : `${table[__table_ts.Table.Symbol.Schema]}.${table[__table_ts.Table.Symbol.Name]}`) !== tableName) {
										abort = true;
										break;
									}
									newChunks[i] = __sql_sql_ts.sql.identifier(c.name);
								} else newChunks[i] = c;
							}
							if (abort) chunks.push(field.sql);
							else {
								const newSql = new __sql_sql_ts.SQL(newChunks);
								if (field.sql.shouldInlineParams) newSql.inlineParams();
								chunks.push(newSql);
							}
						} else chunks.push(field.sql);
						chunks.push(__sql_sql_ts.sql` as ${__sql_sql_ts.sql.identifier(field.fieldAlias)}`);
					}
					break;
				case "SQL":
					if (isSingleTable && tableName !== void 0) {
						const { queryChunks } = field;
						const newChunks = new Array(queryChunks.length);
						let abort = false;
						for (let i = 0; i < queryChunks.length; ++i) {
							const c = queryChunks[i];
							if ((0, __entity_ts.is)(c, __column_ts.Column)) {
								const { table } = c;
								if ((table[__table_ts.Table.Symbol.IsAlias] || table[__table_ts.Table.Symbol.Schema] === void 0 ? table[__table_ts.Table.Symbol.Name] : `${table[__table_ts.Table.Symbol.Schema]}.${table[__table_ts.Table.Symbol.Name]}`) !== tableName) {
									abort = true;
									break;
								}
								newChunks[i] = __sql_sql_ts.sql.identifier(c.name);
							} else newChunks[i] = c;
						}
						if (abort) chunks.push(field);
						else {
							const newSql = new __sql_sql_ts.SQL(newChunks);
							if (field.shouldInlineParams) newSql.inlineParams();
							chunks.push(newSql);
						}
					} else chunks.push(field);
					break;
				case "Subquery":
					if (!field._.isWith) chunks.push(__sql_sql_ts.sql`(${field._.sql}) ${__sql_sql_ts.sql.identifier(field._.alias)}`);
					else chunks.push(field);
					break;
			}
			if (i < columnsLen - 1) chunks.push(new __sql_sql_ts.StringChunk(", "));
		}
		return new __sql_sql_ts.SQL(chunks);
	}
	buildLimit(limit) {
		return typeof limit === "object" || typeof limit === "number" && limit >= 0 ? __sql_sql_ts.sql` limit ${limit}` : void 0;
	}
	buildOrderBy(orderBy) {
		return orderBy && orderBy.length > 0 ? __sql_sql_ts.sql` order by ${__sql_sql_ts.sql.join(orderBy, new __sql_sql_ts.StringChunk(", "))}` : void 0;
	}
	buildSelectQuery({ withList, fieldsFlat, where, having, table, joins, orderBy, groupBy, limit, offset, lockingClause, distinct, setOperators }) {
		if (!fieldsFlat) throw new Error("Select query builder must be provided with `fieldsFlat` on `buildSelectQuery` invocation");
		const fieldsList = fieldsFlat;
		for (const f of fieldsList) if ((0, __entity_ts.is)(f.field, __column_ts.Column) && (0, __table_ts.getTableName)(f.field.table) !== ((0, __entity_ts.is)(table, __subquery_ts.Subquery) ? table._.alias : (0, __entity_ts.is)(table, __sql_sql_ts.SQL) ? void 0 : (0, __table_ts.getTableName)(table)) && !((table) => joins?.some(({ alias }) => alias === (table[__table_ts.Table.Symbol.IsAlias] ? (0, __table_ts.getTableName)(table) : table[__table_ts.Table.Symbol.BaseName])))(f.field.table)) {
			const tableName = (0, __table_ts.getTableName)(f.field.table);
			throw new Error(`Your "${f.path.join("->")}" field references a column "${tableName}"."${f.field.name}", but the table "${tableName}" is not part of the query! Did you forget to join it?`);
		}
		const isSingleTable = !joins || joins.length === 0;
		const withSql = this.buildWithCTE(withList);
		const distinctSql = distinct ? __sql_sql_ts.sql` distinct` : void 0;
		const selection = this.buildSelection(fieldsList, {
			isSingleTable,
			table
		});
		const tableSql = (() => {
			if ((0, __entity_ts.is)(table, __table_ts.Table) && table[__table_ts.Table.Symbol.IsAlias]) return __sql_sql_ts.sql`${__sql_sql_ts.sql`${__sql_sql_ts.sql.identifier(table[__table_ts.Table.Symbol.Schema] ?? "")}.`.if(table[__table_ts.Table.Symbol.Schema])}${__sql_sql_ts.sql.identifier(table[__table_ts.Table.Symbol.OriginalName])} ${__sql_sql_ts.sql.identifier(table[__table_ts.Table.Symbol.Name])}`;
			if ((0, __entity_ts.is)(table, __sql_sql_ts.View) && table[__view_common_ts.ViewBaseConfig].isAlias) {
				let fullName = __sql_sql_ts.sql`${__sql_sql_ts.sql.identifier(table[__view_common_ts.ViewBaseConfig].originalName)}`;
				if (table[__view_common_ts.ViewBaseConfig].schema) fullName = __sql_sql_ts.sql`${__sql_sql_ts.sql.identifier(table[__view_common_ts.ViewBaseConfig].schema)}.${fullName}`;
				return __sql_sql_ts.sql`${fullName} ${__sql_sql_ts.sql.identifier(table[__view_common_ts.ViewBaseConfig].name)}`;
			}
			return table;
		})();
		const joinsArray = [];
		if (joins) for (const [index, joinMeta] of joins.entries()) {
			if (index === 0) joinsArray.push(new __sql_sql_ts.StringChunk(" "));
			const table = joinMeta.table;
			const lateralSql = joinMeta.lateral ? __sql_sql_ts.sql` lateral` : void 0;
			const onSql = joinMeta.on ? __sql_sql_ts.sql` on ${joinMeta.on}` : void 0;
			if ((0, __entity_ts.is)(table, require_singlestore_core_table.SingleStoreTable)) {
				const tableName = table[require_singlestore_core_table.SingleStoreTable.Symbol.Name];
				const tableSchema = table[require_singlestore_core_table.SingleStoreTable.Symbol.Schema];
				const origTableName = table[require_singlestore_core_table.SingleStoreTable.Symbol.OriginalName];
				const alias = tableName === origTableName ? void 0 : joinMeta.alias;
				joinsArray.push(__sql_sql_ts.sql`${new __sql_sql_ts.StringChunk(joinMeta.joinType)} join${lateralSql} ${tableSchema ? __sql_sql_ts.sql`${__sql_sql_ts.sql.identifier(tableSchema)}.` : void 0}${__sql_sql_ts.sql.identifier(origTableName)}${alias && __sql_sql_ts.sql` ${__sql_sql_ts.sql.identifier(alias)}`}${onSql}`);
			} else if ((0, __entity_ts.is)(table, __sql_sql_ts.View)) {
				const viewName = table[__view_common_ts.ViewBaseConfig].name;
				const viewSchema = table[__view_common_ts.ViewBaseConfig].schema;
				const origViewName = table[__view_common_ts.ViewBaseConfig].originalName;
				const alias = viewName === origViewName ? void 0 : joinMeta.alias;
				joinsArray.push(__sql_sql_ts.sql`${new __sql_sql_ts.StringChunk(joinMeta.joinType)} join${lateralSql} ${viewSchema ? __sql_sql_ts.sql`${__sql_sql_ts.sql.identifier(viewSchema)}.` : void 0}${__sql_sql_ts.sql.identifier(origViewName)}${alias && __sql_sql_ts.sql` ${__sql_sql_ts.sql.identifier(alias)}`}${onSql}`);
			} else joinsArray.push(__sql_sql_ts.sql`${new __sql_sql_ts.StringChunk(joinMeta.joinType)} join${lateralSql} ${table}${onSql}`);
			if (index < joins.length - 1) joinsArray.push(new __sql_sql_ts.StringChunk(" "));
		}
		const joinsSql = new __sql_sql_ts.SQL(joinsArray);
		const whereSql = where ? __sql_sql_ts.sql` where ${where}` : void 0;
		const havingSql = having ? __sql_sql_ts.sql` having ${having}` : void 0;
		const orderBySql = this.buildOrderBy(orderBy);
		const groupBySql = groupBy && groupBy.length > 0 ? __sql_sql_ts.sql` group by ${__sql_sql_ts.sql.join(groupBy, new __sql_sql_ts.StringChunk(", "))}` : void 0;
		const limitSql = this.buildLimit(limit);
		const offsetSql = offset ? __sql_sql_ts.sql` offset ${offset}` : void 0;
		let lockingClausesSql;
		if (lockingClause) {
			const { config, strength } = lockingClause;
			lockingClausesSql = __sql_sql_ts.sql` for ${new __sql_sql_ts.StringChunk(strength)}`;
			if (config.noWait) lockingClausesSql.append(__sql_sql_ts.sql` nowait`);
			else if (config.skipLocked) lockingClausesSql.append(__sql_sql_ts.sql` skip locked`);
		}
		const finalQuery = __sql_sql_ts.sql`${withSql}select${distinctSql} ${selection} from ${tableSql}${joinsSql}${whereSql}${groupBySql}${havingSql}${orderBySql}${limitSql}${offsetSql}${lockingClausesSql}`;
		if (setOperators.length > 0) return this.buildSetOperations(finalQuery, setOperators);
		return finalQuery;
	}
	buildSetOperations(leftSelect, setOperators) {
		const [setOperator, ...rest] = setOperators;
		if (!setOperator) throw new Error("Cannot pass undefined values to any set operator");
		if (rest.length === 0) return this.buildSetOperationQuery({
			leftSelect,
			setOperator
		});
		return this.buildSetOperations(this.buildSetOperationQuery({
			leftSelect,
			setOperator
		}), rest);
	}
	buildSetOperationQuery({ leftSelect, setOperator: { type, isAll, rightSelect, limit, orderBy, offset } }) {
		const leftChunk = __sql_sql_ts.sql`(${leftSelect.getSQL()}) `;
		const rightChunk = __sql_sql_ts.sql`(${rightSelect.getSQL()})`;
		let orderBySql;
		if (orderBy && orderBy.length > 0) {
			const orderByValues = [];
			for (const orderByUnit of orderBy) if ((0, __entity_ts.is)(orderByUnit, require_singlestore_core_columns_common.SingleStoreColumn)) orderByValues.push(__sql_sql_ts.sql.identifier(orderByUnit.name));
			else if ((0, __entity_ts.is)(orderByUnit, __sql_sql_ts.SQL)) {
				for (let i = 0; i < orderByUnit.queryChunks.length; i++) {
					const chunk = orderByUnit.queryChunks[i];
					if ((0, __entity_ts.is)(chunk, require_singlestore_core_columns_common.SingleStoreColumn)) orderByUnit.queryChunks[i] = __sql_sql_ts.sql.identifier(chunk.name);
				}
				orderByValues.push(__sql_sql_ts.sql`${orderByUnit}`);
			} else orderByValues.push(__sql_sql_ts.sql`${orderByUnit}`);
			orderBySql = __sql_sql_ts.sql` order by ${__sql_sql_ts.sql.join(orderByValues, new __sql_sql_ts.StringChunk(", "))} `;
		}
		const limitSql = typeof limit === "object" || typeof limit === "number" && limit >= 0 ? __sql_sql_ts.sql` limit ${limit}` : void 0;
		const operatorChunk = new __sql_sql_ts.StringChunk(`${type} ${isAll ? "all " : ""}`);
		const offsetSql = offset ? __sql_sql_ts.sql` offset ${offset}` : void 0;
		return __sql_sql_ts.sql`${leftChunk}${operatorChunk}${rightChunk}${orderBySql}${limitSql}${offsetSql}`;
	}
	buildInsertQuery({ table, values, ignore, onConflict, columnList }) {
		const columns = table[__table_ts.Table.Symbol.Columns];
		const colEntries = columnList ? columnList.map((name) => [name, columns[name]]) : Object.entries(columns).filter(([_, col]) => !col.shouldDisableInsert());
		const insertOrderArr = new Array(colEntries.length * 2 + 1);
		let orderWriteIdx = 0;
		insertOrderArr[orderWriteIdx++] = new __sql_sql_ts.StringChunk("(");
		for (let i = 0; i < colEntries.length; ++i) {
			const [, { name }] = colEntries[i];
			insertOrderArr[orderWriteIdx++] = __sql_sql_ts.sql.identifier(name);
			if (i < colEntries.length - 1) insertOrderArr[orderWriteIdx++] = new __sql_sql_ts.StringChunk(", ");
		}
		insertOrderArr[orderWriteIdx++] = new __sql_sql_ts.StringChunk(")");
		const insertOrder = new __sql_sql_ts.SQL(insertOrderArr);
		const generatedIdsResponse = [];
		const valuesSqlList = Array.from({ length: (colEntries.length * 2 + 1) * values.length + values.length - 1 });
		let writeIdx = 0;
		for (let valueIndex = 0; valueIndex < values.length; ++valueIndex) {
			const value = values[valueIndex];
			const generatedIds = {};
			valuesSqlList[writeIdx++] = new __sql_sql_ts.StringChunk("(");
			for (let i = 0; i < colEntries.length; ++i) {
				const [fieldName, col] = colEntries[i];
				const colValue = value[fieldName];
				if (colValue === void 0) if (col.defaultFn !== void 0) {
					const defaultFnResult = col.defaultFn();
					generatedIds[fieldName] = defaultFnResult;
					const defaultValue = (0, __entity_ts.is)(defaultFnResult, __sql_sql_ts.SQL) ? defaultFnResult : __sql_sql_ts.sql.param(defaultFnResult, col);
					valuesSqlList[writeIdx++] = defaultValue;
				} else if (!col.default && col.onUpdateFn !== void 0) {
					const onUpdateFnResult = col.onUpdateFn();
					const newValue = (0, __entity_ts.is)(onUpdateFnResult, __sql_sql_ts.SQL) ? onUpdateFnResult : __sql_sql_ts.sql.param(onUpdateFnResult, col);
					valuesSqlList[writeIdx++] = newValue;
				} else valuesSqlList[writeIdx++] = new __sql_sql_ts.StringChunk(`default`);
				else if ((0, __entity_ts.is)(colValue, __sql_sql_ts.SQL)) valuesSqlList[writeIdx++] = colValue;
				else {
					if (col.defaultFn) generatedIds[fieldName] = colValue;
					valuesSqlList[writeIdx++] = new __sql_sql_ts.Param(colValue, col);
				}
				if (i < colEntries.length - 1) valuesSqlList[writeIdx++] = new __sql_sql_ts.StringChunk(", ");
			}
			generatedIdsResponse.push(generatedIds);
			valuesSqlList[writeIdx++] = new __sql_sql_ts.StringChunk(")");
			if (valueIndex < values.length - 1) valuesSqlList[writeIdx++] = new __sql_sql_ts.StringChunk(`, `);
		}
		const valuesSql = new __sql_sql_ts.SQL(valuesSqlList);
		return {
			sql: __sql_sql_ts.sql`insert${ignore ? __sql_sql_ts.sql` ignore` : void 0} into ${table} ${insertOrder} values ${valuesSql}${onConflict ? __sql_sql_ts.sql` on duplicate key ${onConflict}` : void 0}`,
			generatedIds: generatedIdsResponse
		};
	}
	sqlToQuery(sql, invokeSource) {
		return sql.toQuery({
			escapeName: this.escapeName,
			escapeParam: this.escapeParam,
			escapeString: this.escapeString,
			invokeSource
		});
	}
	_buildRelationalQuery({ fullSchema, schema, tableNamesMap, table, tableConfig, queryConfig: config, tableAlias, nestedQueryRelation, joinOn }) {
		let selection = [];
		let limit, offset, orderBy, where;
		const joins = [];
		if (config === true) selection = Object.entries(tableConfig.columns).map(([key, value]) => ({
			dbKey: value.name,
			tsKey: key,
			field: (0, __alias_ts.aliasedTableColumn)(value, tableAlias),
			relationTableTsKey: void 0,
			isJson: false,
			selection: []
		}));
		else {
			const aliasedColumns = Object.fromEntries(Object.entries(tableConfig.columns).map(([key, value]) => [key, (0, __alias_ts.aliasedTableColumn)(value, tableAlias)]));
			if (config.where) {
				const whereSql = typeof config.where === "function" ? config.where(aliasedColumns, ___relations_ts.getOperators()) : config.where;
				where = whereSql && (0, __alias_ts.mapColumnsInSQLToAlias)(whereSql, tableAlias);
			}
			const fieldsSelection = [];
			let selectedColumns = [];
			if (config.columns) {
				let isIncludeMode = false;
				for (const [field, value] of Object.entries(config.columns)) {
					if (value === void 0) continue;
					if (field in tableConfig.columns) {
						if (!isIncludeMode && value === true) isIncludeMode = true;
						selectedColumns.push(field);
					}
				}
				if (selectedColumns.length > 0) selectedColumns = isIncludeMode ? selectedColumns.filter((c) => config.columns?.[c] === true) : Object.keys(tableConfig.columns).filter((key) => !selectedColumns.includes(key));
			} else selectedColumns = Object.keys(tableConfig.columns);
			for (const field of selectedColumns) {
				const column = tableConfig.columns[field];
				fieldsSelection.push({
					tsKey: field,
					value: column
				});
			}
			let selectedRelations = [];
			if (config.with) selectedRelations = Object.entries(config.with).filter((entry) => !!entry[1]).map(([tsKey, queryConfig]) => ({
				tsKey,
				queryConfig,
				relation: tableConfig.relations[tsKey]
			}));
			let extras;
			if (config.extras) {
				extras = typeof config.extras === "function" ? config.extras(aliasedColumns, { sql: __sql_sql_ts.sql }) : config.extras;
				for (const [tsKey, value] of Object.entries(extras)) fieldsSelection.push({
					tsKey,
					value: (0, __alias_ts.mapColumnsInAliasedSQLToAlias)(value, tableAlias)
				});
			}
			for (const { tsKey, value } of fieldsSelection) selection.push({
				dbKey: (0, __entity_ts.is)(value, __sql_sql_ts.SQL.Aliased) ? value.fieldAlias : tableConfig.columns[tsKey].name,
				tsKey,
				field: (0, __entity_ts.is)(value, __column_ts.Column) ? (0, __alias_ts.aliasedTableColumn)(value, tableAlias) : value,
				relationTableTsKey: void 0,
				isJson: false,
				selection: []
			});
			let orderByOrig = typeof config.orderBy === "function" ? config.orderBy(aliasedColumns, ___relations_ts.getOrderByOperators()) : config.orderBy ?? [];
			if (!Array.isArray(orderByOrig)) orderByOrig = [orderByOrig];
			orderBy = orderByOrig.map((orderByValue) => {
				if ((0, __entity_ts.is)(orderByValue, __column_ts.Column)) return (0, __alias_ts.aliasedTableColumn)(orderByValue, tableAlias);
				return (0, __alias_ts.mapColumnsInSQLToAlias)(orderByValue, tableAlias);
			});
			limit = config.limit;
			offset = config.offset;
			for (const { tsKey: selectedRelationTsKey, queryConfig: selectedRelationConfigValue, relation } of selectedRelations) {
				const normalizedRelation = ___relations_ts.normalizeRelation(schema, tableNamesMap, relation);
				const relationTableTsName = tableNamesMap[(0, __table_ts.getTableUniqueName)(relation.referencedTable)];
				const relationTableAlias = `${tableAlias}_${selectedRelationTsKey}`;
				const joinOn = (0, __sql_expressions_index_ts.and)(...normalizedRelation.fields.map((field, i) => (0, __sql_expressions_index_ts.eq)((0, __alias_ts.aliasedTableColumn)(normalizedRelation.references[i], relationTableAlias), (0, __alias_ts.aliasedTableColumn)(field, tableAlias))));
				const builtRelation = this._buildRelationalQuery({
					fullSchema,
					schema,
					tableNamesMap,
					table: fullSchema[relationTableTsName],
					tableConfig: schema[relationTableTsName],
					queryConfig: (0, __entity_ts.is)(relation, ___relations_ts.One) ? selectedRelationConfigValue === true ? { limit: 1 } : {
						...selectedRelationConfigValue,
						limit: 1
					} : selectedRelationConfigValue,
					tableAlias: relationTableAlias,
					joinOn,
					nestedQueryRelation: relation
				});
				const field = __sql_sql_ts.sql`${__sql_sql_ts.sql.identifier(relationTableAlias)}.${__sql_sql_ts.sql.identifier("data")}`.as(selectedRelationTsKey);
				joins.push({
					on: __sql_sql_ts.sql`true`,
					table: new __subquery_ts.Subquery(builtRelation.sql, {}, relationTableAlias),
					alias: relationTableAlias,
					joinType: "left",
					lateral: true
				});
				selection.push({
					dbKey: selectedRelationTsKey,
					tsKey: selectedRelationTsKey,
					field,
					relationTableTsKey: relationTableTsName,
					isJson: true,
					selection: builtRelation.selection
				});
			}
		}
		if (selection.length === 0) throw new __errors_ts.DrizzleError({ message: `No fields selected for table "${tableConfig.tsName}" ("${tableAlias}")` });
		let result;
		where = (0, __sql_expressions_index_ts.and)(joinOn, where);
		if (nestedQueryRelation) {
			let field = __sql_sql_ts.sql`JSON_TO_ARRAY(${__sql_sql_ts.sql.join(selection.map(({ field, tsKey, isJson }) => isJson ? __sql_sql_ts.sql`${__sql_sql_ts.sql.identifier(`${tableAlias}_${tsKey}`)}.${__sql_sql_ts.sql.identifier("data")}` : (0, __entity_ts.is)(field, __sql_sql_ts.SQL.Aliased) ? field.sql : field), new __sql_sql_ts.StringChunk(", "))})`;
			if ((0, __entity_ts.is)(nestedQueryRelation, ___relations_ts.Many)) field = __sql_sql_ts.sql`json_agg(${field})`;
			const nestedSelection = [{
				dbKey: "data",
				tsKey: "data",
				field: field.as("data"),
				isJson: true,
				relationTableTsKey: tableConfig.tsName,
				selection
			}];
			if (limit !== void 0 || offset !== void 0 || (orderBy?.length ?? 0) > 0) {
				result = this.buildSelectQuery({
					table: (0, __alias_ts.aliasedTable)(table, tableAlias),
					fields: {},
					fieldsFlat: [{
						path: [],
						field: __sql_sql_ts.sql.raw("*"),
						fieldType: "SQL"
					}, ...(orderBy?.length ?? 0) > 0 ? [{
						path: [],
						field: __sql_sql_ts.sql`row_number() over (order by ${__sql_sql_ts.sql.join(orderBy, new __sql_sql_ts.StringChunk(", "))})`,
						fieldType: "SQL"
					}] : []],
					where,
					limit,
					offset,
					setOperators: []
				});
				where = void 0;
				limit = void 0;
				offset = void 0;
				orderBy = void 0;
			} else result = (0, __alias_ts.aliasedTable)(table, tableAlias);
			result = this.buildSelectQuery({
				table: (0, __entity_ts.is)(result, require_singlestore_core_table.SingleStoreTable) ? result : new __subquery_ts.Subquery(result, {}, tableAlias),
				fields: {},
				fieldsFlat: nestedSelection.map(({ field }) => {
					const mapped = (0, __entity_ts.is)(field, __column_ts.Column) ? (0, __alias_ts.aliasedTableColumn)(field, tableAlias) : field;
					return {
						path: [],
						field: mapped,
						fieldType: (0, __entity_ts.is)(mapped, __column_ts.Column) ? "Column" : (0, __entity_ts.is)(mapped, __sql_sql_ts.SQL.Aliased) ? "SQL.Aliased" : "SQL"
					};
				}),
				joins,
				where,
				limit,
				offset,
				orderBy,
				setOperators: []
			});
		} else result = this.buildSelectQuery({
			table: (0, __alias_ts.aliasedTable)(table, tableAlias),
			fields: {},
			fieldsFlat: selection.map(({ field }) => {
				const mapped = (0, __entity_ts.is)(field, __column_ts.Column) ? (0, __alias_ts.aliasedTableColumn)(field, tableAlias) : field;
				return {
					path: [],
					field: mapped,
					fieldType: (0, __entity_ts.is)(mapped, __column_ts.Column) ? "Column" : (0, __entity_ts.is)(mapped, __sql_sql_ts.SQL.Aliased) ? "SQL.Aliased" : "SQL"
				};
			}),
			joins,
			where,
			limit,
			offset,
			orderBy,
			setOperators: []
		});
		return {
			tableTsKey: tableConfig.tsName,
			sql: result,
			selection
		};
	}
	buildRqbColumn(table, field, key, selection, tableTsName) {
		let fieldType;
		let output;
		if ((0, __entity_ts.is)(field, __column_ts.Column)) {
			fieldType = "Column";
			const name = __sql_sql_ts.sql`${table}.${__sql_sql_ts.sql.identifier(field.name)}`;
			switch (field.columnType) {
				case "SingleStoreBinary":
				case "SingleStoreVarBinary":
				case "SingleStoreTime":
				case "SingleStoreDateTimeString":
				case "SingleStoreTimestampString":
				case "SingleStoreFloat":
				case "SingleStoreDecimal":
				case "SingleStoreDecimalNumber":
				case "SingleStoreDecimalBigInt":
				case "SingleStoreBigInt64":
				case "SingleStoreBigIntString":
				case "SingleStoreEnumColumn":
					output = __sql_sql_ts.sql`cast(${name} as char) as ${__sql_sql_ts.sql.identifier(key)}`;
					break;
				case "SingleStoreVector":
				case "SingleStoreBigIntVector":
					output = __sql_sql_ts.sql`hex(${name} :> blob) as ${__sql_sql_ts.sql.identifier(key)}`;
					break;
				case "SingleStoreCustomColumn":
					output = __sql_sql_ts.sql`${field.jsonSelectIdentifier(name, __sql_sql_ts.sql)} as ${__sql_sql_ts.sql.identifier(key)}`;
					break;
				default: output = __sql_sql_ts.sql`${name} as ${__sql_sql_ts.sql.identifier(key)}`;
			}
		} else if ((0, __entity_ts.is)(field, __sql_sql_ts.SQL)) {
			fieldType = "SQL";
			output = __sql_sql_ts.sql`${table}.${__sql_sql_ts.sql.identifier(key)} as ${__sql_sql_ts.sql.identifier(key)}`;
		} else if ((0, __entity_ts.is)(field, __sql_sql_ts.SQL.Aliased)) {
			fieldType = "SQL.Aliased";
			output = __sql_sql_ts.sql`${table}.${__sql_sql_ts.sql.identifier(field.fieldAlias)} as ${__sql_sql_ts.sql.identifier(key)}`;
		} else if ((0, __sql_sql_ts.isSQLWrapper)(field)) {
			fieldType = "SQLWrapper";
			output = __sql_sql_ts.sql`${table}.${__sql_sql_ts.sql.identifier(key)} as ${__sql_sql_ts.sql.identifier(key)}`;
		} else throw new __errors_ts.DrizzleError({ message: field === void 0 ? `Unknown column: "${tableTsName}"."${key}"` : `Views with nested selections are not supported by the relational query builder` });
		selection.push({
			key,
			field,
			fieldType
		});
		return output;
	}
	getSelectedTableColumns = (table, columns) => {
		const selectedColumns = [];
		const columnContainer = table[__table_ts.TableColumns];
		const entries = Object.entries(columns);
		let colSelectionMode;
		for (const [k, v] of entries) {
			if (v === void 0) continue;
			colSelectionMode = colSelectionMode || v;
			if (v) {
				const column = columnContainer[k];
				selectedColumns.push({
					column,
					tsName: k
				});
			}
		}
		if (colSelectionMode === false) for (const [k, v] of Object.entries(columnContainer)) {
			if (columns[k] === false) continue;
			selectedColumns.push({
				column: v,
				tsName: k
			});
		}
		return selectedColumns;
	};
	buildColumns = (table, selection, tableTsName, config) => {
		if (!config?.columns) return __sql_sql_ts.sql.join(Object.entries(table[__table_ts.TableColumns]).map(([k, v]) => {
			return this.buildRqbColumn(table, v, k, selection, tableTsName);
		}), new __sql_sql_ts.StringChunk(", "));
		const columnIdentifiers = [];
		const selectedColumns = this.getSelectedTableColumns(table, config.columns);
		for (const { column, tsName } of selectedColumns) columnIdentifiers.push(this.buildRqbColumn(table, column, tsName, selection, tableTsName));
		return columnIdentifiers.length ? __sql_sql_ts.sql.join(columnIdentifiers, new __sql_sql_ts.StringChunk(", ")) : void 0;
	};
	buildRelationalQuery({ schema, table, tableConfig, queryConfig: config, relationWhere, mode, errorPath, depth, isNestedMany, throughJoin }) {
		const selection = [];
		const isSingle = mode === "first";
		const params = config === true ? void 0 : config;
		const currentPath = errorPath ?? "";
		const currentDepth = depth ?? 0;
		if (!currentDepth) table = (0, __alias_ts.aliasedTable)(table, `d${currentDepth}`);
		const limit = isSingle ? 1 : params?.limit;
		const offset = params?.offset;
		const columns = this.buildColumns(table, selection, tableConfig.name, params);
		const where = params && "where" in params && relationWhere ? (0, __sql_expressions_index_ts.and)((0, __relations_ts.relationsFilterToSQL)(table, params.where, tableConfig.relations, schema), relationWhere) : params && "where" in params ? (0, __relations_ts.relationsFilterToSQL)(table, params.where, tableConfig.relations, schema) : relationWhere;
		const order = params?.orderBy ? (0, __relations_ts.relationsOrderToSQL)(table, params.orderBy) : void 0;
		const extras = params?.extras ? (0, __relations_ts.relationExtrasToSQL)(table, params.extras) : void 0;
		if (extras) selection.push(...extras.selection);
		const selectionArr = columns ? [columns] : [];
		if (extras?.sql) selectionArr.push(extras.sql);
		let joins;
		switch (params) {
			case void 0: break;
			default: {
				const { with: withParam } = params;
				if (!withParam) break;
				const withEntries = Object.entries(withParam).filter(([_, v]) => v);
				if (!withEntries.length) break;
				const joinChunks = new Array(withEntries.length * 2);
				joinChunks[0] = new __sql_sql_ts.StringChunk(" ");
				for (let readIdx = 0, writeIdx = 1; readIdx < withEntries.length; ++readIdx) {
					const [k, join] = withEntries[readIdx];
					const relation = tableConfig.relations[k];
					if (!relation) throw new __errors_ts.DrizzleError({ message: `Unknown relation "${tableConfig.name}" -> "${k}"` });
					const isSingle = relation.relationType === "one";
					selectionArr.push(isSingle ? __sql_sql_ts.sql`${__sql_sql_ts.sql.identifier(k)}.${__sql_sql_ts.sql.identifier("r")} as ${__sql_sql_ts.sql.identifier(k)}` : __sql_sql_ts.sql`coalesce(${__sql_sql_ts.sql.identifier(k)}.${__sql_sql_ts.sql.identifier("r")}, json_build_array()) as ${__sql_sql_ts.sql.identifier(k)}`);
					const targetTable = (0, __alias_ts.aliasedTable)(relation.targetTable, `d${currentDepth + 1}`);
					const throughTable = relation.throughTable ? (0, __alias_ts.aliasedTable)(relation.throughTable, `tr${currentDepth}`) : void 0;
					const { filter, joinCondition } = (0, __relations_ts.relationToSQL)(relation, table, targetTable, throughTable);
					const throughJoin = throughTable ? __sql_sql_ts.sql` inner join ${(0, __relations_ts.getTableAsAliasSQL)(throughTable)} on ${joinCondition}` : void 0;
					const innerQuery = this.buildRelationalQuery({
						table: targetTable,
						mode: isSingle ? "first" : "many",
						schema,
						queryConfig: join,
						tableConfig: schema[relation.targetTableName],
						relationWhere: filter,
						errorPath: `${currentPath.length ? `${currentPath}.` : ""}${k}`,
						depth: currentDepth + 1,
						isNestedMany: !isSingle,
						throughJoin
					});
					selection.push({
						field: targetTable,
						fieldType: "Nested",
						key: k,
						selection: innerQuery.selection,
						isArray: !isSingle,
						isOptional: (relation.optional ?? false) || join !== true && !!join.where
					});
					const jsonColumns = __sql_sql_ts.sql.join(innerQuery.selection.map((s) => __sql_sql_ts.sql`${new __sql_sql_ts.StringChunk(this.escapeString(s.key))}, ${__sql_sql_ts.sql.identifier(s.key)}`), new __sql_sql_ts.StringChunk(", "));
					const joinQuery = __sql_sql_ts.sql`left join lateral(select ${__sql_sql_ts.sql`${isSingle ? __sql_sql_ts.sql`json_build_object(${jsonColumns})` : __sql_sql_ts.sql`json_agg(json_build_object(${jsonColumns})${innerQuery.order ? __sql_sql_ts.sql` ORDER BY ${__sql_sql_ts.sql.identifier(`$drizzle_order_row_number`)}` : void 0})`} as ${__sql_sql_ts.sql.identifier("r")}`} from (${innerQuery.sql}) as ${__sql_sql_ts.sql.identifier("t")}) as ${__sql_sql_ts.sql.identifier(k)} on true`;
					joinChunks[writeIdx++] = joinQuery;
					if (readIdx < withEntries.length - 1) joinChunks[writeIdx++] = new __sql_sql_ts.StringChunk(" ");
				}
				joins = new __sql_sql_ts.SQL(joinChunks);
				break;
			}
		}
		if (!selectionArr.length) throw new __errors_ts.DrizzleError({ message: `No fields selected for table "${tableConfig.name}"${currentPath ? ` ("${currentPath}")` : ""}` });
		if (isNestedMany && order) selectionArr.push(__sql_sql_ts.sql`row_number() over (order by ${order}) as ${__sql_sql_ts.sql.identifier(`$drizzle_order_row_number`)}`);
		return {
			sql: __sql_sql_ts.sql`select ${__sql_sql_ts.sql.join(selectionArr, new __sql_sql_ts.StringChunk(", "))} from ${(0, __relations_ts.getTableAsAliasSQL)(table)}${throughJoin}${joins}${__sql_sql_ts.sql` where ${where}`.if(where)}${__sql_sql_ts.sql` order by ${order}`.if(order)}${__sql_sql_ts.sql` limit ${limit}`.if(limit !== void 0)}${__sql_sql_ts.sql` offset ${offset}`.if(offset !== void 0)}`,
			selection,
			order
		};
	}
};

//#endregion
exports.SingleStoreDialect = SingleStoreDialect;
//# sourceMappingURL=dialect.cjs.map